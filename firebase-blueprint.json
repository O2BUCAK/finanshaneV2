{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "App user profile information.",
      "type": "object",
      "properties": {
        "fullName": { "type": "string", "description": "Full name of the user." },
        "email": { "type": "string", "format": "email", "description": "User's email address." },
        "avatarUrl": { "type": "string", "description": "URL to user's profile picture." },
        "activeHouseholdId": { "type": "string", "description": "Current household ID." },
        "joinedBudgetIds": { "type": "array", "items": { "type": "string" }, "description": "List of shared budget IDs the user is part of." },
        "role": { "type": "string", "enum": ["adult", "child", "elderly", "other"], "description": "User's role in the household." },
        "isAdmin": { "type": "boolean", "description": "Whether the user is an admin." },
        "kvkkAccepted": { "type": "boolean", "description": "Whether KVKK terms are accepted." },
        "kvkkAcceptedAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["fullName", "email", "createdAt"]
    },
    "Household": {
      "title": "Household",
      "description": "A group of users sharing finances.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Name of the household." },
        "ownerId": { "type": "string", "description": "UID of the household creator." },
        "currency": { "type": "string", "description": "Default currency (e.g., TRY)." },
        "joinCode": { "type": "string", "description": "Code to join the household." },
        "members": { "type": "object", "description": "Map of userIds to roles/metadata." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "ownerId", "members", "joinCode", "createdAt"]
    },
    "Account": {
      "title": "Account",
      "description": "Financial account (Asset, Liability, Income, Expense).",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerId": { "type": "string", "description": "UID of the account owner." },
        "institution": { "type": "string" },
        "type": { "type": "string", "enum": ["asset", "liability", "income", "expense"] },
        "branch": { "type": "string", "enum": ["banking", "crypto", "social_gift"] },
        "subType": { "type": "string" },
        "balance": { "type": "number" },
        "currency": { "type": "string" },
        "assetDetails": { "type": "object" },
        "icon": { "type": "string" },
        "color": { "type": "string" },
        "isCreditCard": { "type": "boolean" },
        "creditLimit": { "type": "number" },
        "statementDay": { "type": "number" },
        "statementBalance": { "type": "number" },
        "minimumPayment": { "type": "number" },
        "isArchived": { "type": "boolean" },
        "depositDetails": { "type": "object" },
        "points": { "type": "array", "items": { "type": "object" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "ownerId", "type", "branch", "balance", "currency", "createdAt"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "A double-entry financial record.",
      "type": "object",
      "properties": {
        "description": { "type": "string" },
        "amount": { "type": "number" },
        "currency": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "debitAccountId": { "type": "string" },
        "creditAccountId": { "type": "string" },
        "categoryId": { "type": "string" },
        "userId": { "type": "string" },
        "notes": { "type": "string" },
        "receiptUrl": { "type": "string" },
        "isInstallment": { "type": "boolean" },
        "installmentCount": { "type": "number" },
        "installmentNumber": { "type": "number" },
        "parentTransactionId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["description", "amount", "currency", "date", "debitAccountId", "creditAccountId", "categoryId", "userId", "createdAt", "updatedAt"]
    },
    "Category": {
      "title": "Category",
      "description": "Transaction category.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["income", "expense"] },
        "icon": { "type": "string" },
        "color": { "type": "string" },
        "isSystem": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "type", "createdAt"]
    },
    "IncomeSource": {
      "title": "Income Source",
      "description": "A source of income (Fixed, Variable, Spot).",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "ownerId": { "type": "string" },
        "flowType": { "type": "string", "enum": ["fixed", "variable", "spot"] },
        "amount": { "type": "number" },
        "currency": { "type": "string" },
        "targetAccountId": { "type": "string" },
        "periodDay": { "type": "number" },
        "isArchived": { "type": "boolean" },
        "revisions": { "type": "array", "items": { "type": "object" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "ownerId", "flowType", "amount", "currency", "targetAccountId", "createdAt"]
    },
    "ExpectedIncome": {
      "title": "Expected Income",
      "description": "A scheduled or expected income entry.",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "sourceId": { "type": "string" },
        "sourceName": { "type": "string" },
        "amount": { "type": "number" },
        "currency": { "type": "string" },
        "expectedDate": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pending", "realized", "cancelled"] },
        "targetAccountId": { "type": "string" },
        "transactionId": { "type": "string" },
        "isRetroactive": { "type": "boolean" },
        "parentSourceId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerId", "sourceId", "sourceName", "amount", "currency", "expectedDate", "status", "targetAccountId", "createdAt"]
    },
    "PlannedExpense": {
      "title": "Planned Expense",
      "description": "A scheduled or planned expense entry.",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "title": { "type": "string" },
        "amount": { "type": "number" },
        "currency": { "type": "string" },
        "dueDate": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pending", "paid"] },
        "categoryId": { "type": "string" },
        "sourceAccountId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerId", "title", "amount", "currency", "dueDate", "status", "categoryId", "createdAt"]
    },
    "SharedBudget": {
      "title": "Shared Budget",
      "description": "A shared budget for tracking group expenses.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "joinCode": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "participants": { "type": "array", "items": { "type": "object" } },
        "expenses": { "type": "array", "items": { "type": "object" } },
        "isSettled": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "date", "participants", "expenses", "isSettled", "createdAt"]
    },
    "JoinRequest": {
      "title": "Join Request",
      "description": "A request to join a household.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "UID of the user requesting to join." },
        "householdId": { "type": "string", "description": "ID of the household to join." },
        "householdName": { "type": "string", "description": "Name of the household." },
        "displayName": { "type": "string", "description": "Display name of the user." },
        "email": { "type": "string", "format": "email", "description": "Email of the user." },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected", "cancelled"], "description": "Status of the request." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "householdId", "displayName", "email", "status", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": { "schema": "UserProfile", "description": "Global user profiles." },
    "/households/{householdId}": { "schema": "Household", "description": "Household groups." },
    "/joinRequests/{requestId}": { "schema": "JoinRequest", "description": "Global join requests." },
    "/households/{householdId}/accounts/{accountId}": { "schema": "Account", "description": "Accounts within a household." },
    "/households/{householdId}/transactions/{transactionId}": { "schema": "Transaction", "description": "Transactions within a household." },
    "/households/{householdId}/categories/{categoryId}": { "schema": "Category", "description": "Categories within a household." },
    "/households/{householdId}/incomeSources/{sourceId}": { "schema": "IncomeSource", "description": "Income sources within a household." },
    "/households/{householdId}/expectedIncomes/{incomeId}": { "schema": "ExpectedIncome", "description": "Expected incomes within a household." },
    "/households/{householdId}/plannedExpenses/{expenseId}": { "schema": "PlannedExpense", "description": "Planned expenses within a household." },
    "/households/{householdId}/sharedBudgets/{budgetId}": { "schema": "SharedBudget", "description": "Shared budgets within a household." }
  }
}
